# Lesson 147
# TCP, HTTP, RPC & API Communication

Important distinctions:

Socket
!=
TCP

TCP
!=
HTTP

HTTP
!=
REST

REST
!=
RPC

RPC
!=
API


Socket:

programming interface
for network communication.


TCP:

reliable ordered
byte stream.


HTTP:

application protocol
for request/response
communication.


REST:

resource-oriented
API design style.


RPC:

remote procedure
call abstraction.


API:

interface exposed
by a service.


Remote Call
!=
Local Function Call


Remote calls may fail because of:

timeout
network failure
server failure
authorization
overload


AI communication patterns:

request-response
server streaming
bidirectional streaming
RPC


Security-sensitive retries
require idempotency.


API Schema
=
Communication Contract